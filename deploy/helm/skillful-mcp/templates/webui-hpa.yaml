{{- if and .Values.webui.enabled .Values.webui.hpa.enabled -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "skillful-mcp.webui.fullname" . }}
  labels:
    {{- include "skillful-mcp.labels" . | nindent 4 }}
    app.kubernetes.io/component: webui
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "skillful-mcp.webui.fullname" . }}
  minReplicas: {{ .Values.webui.hpa.minReplicas }}
  maxReplicas: {{ .Values.webui.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.webui.hpa.targetCPUUtilizationPercentage }}
{{- end -}}
